# Carnatify API runtime deps.
# torch is required only because carnatify.ml.raga_classifier imports it at
# module load (inference itself uses the sklearn .pkl). The CPU wheel keeps the
# image small; see the Dockerfile for the --index-url that selects it.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
python-multipart>=0.0.9      # required by FastAPI for UploadFile / File(...)
slowapi>=0.1.9               # per-IP rate limiting on /predict-audio

numpy>=2.0,<3
scipy>=1.12
scikit-learn==1.9.0          # must match the version that pickled raga_classifier.pkl
joblib>=1.3

torch>=2.2,<2.6               # pinned to avoid torchaudio torchcodec requirement (>=2.6)
librosa>=0.10                 # imported by audio.feature_extractor
dtaidistance>=2.3            # imported by ml.dtw_matcher
demucs>=4.0.0                # vocal source separation for /predict-audio

google-generativeai>=0.8    # lyrics meaning generation (Gemini 2.0 Flash)
essentia>=2.1b6              # TonicIndianArtMusic — drone-based Sa estimation in /predict-audio
